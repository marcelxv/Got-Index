import React from 'react'
import spinner from '../../img/loading.gif'

const Spinner = () => {
  return (
     <img src={spinner} style={{display:'block', margin: 'auto', width: '200px;' }}/>

  ) 
}

export default Spinner